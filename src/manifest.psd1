@{
    PrivateData   = @{
        PSData = @{
            Tags = @(
                'PublicIP'
                'IP'
                'Public'
                'IPConfig'
                'Config'
            )
        }
    }
}

